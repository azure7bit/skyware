<div class='row'>
    <div class='col-md-1'>
        <% image = current_user.avatar.blank? ? "img/profile-placeholder.png" : current_user.avatar.url %>
        <%= image_tag image , :height => "52", :width => "52" %>
    </div>
    <div class='col-md-11'>
        <%= simple_form_for(:conversation, url: :conversations,remote: true, :defaults => { :wrapper_html => {:class => 'form-group'}, :input_html => { :class => 'form-control' } }) do |f| %>
        <form role="form">
            <% if @user.present? %>
                <%= f.hidden_field :recipients, :value => @user.email %>
            <% end %>
            <div class="form-group">
                <%= f.hidden_field :subject, value: post.title %>
            </div>
            <div class="form-group">
                <%= f.text_area :body, :rows => 3, :class => "form-control", placeholder: "POST CONTENT" %>
            </div>
            <div class="smallMenuItems">
                <%= f.submit "POST TO HQ", class: 'btn btn-success' %>
            </div>
        </form>
        <% end %>
    </div>
</div>