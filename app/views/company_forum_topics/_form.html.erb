<%= simple_form_for([@company_forum, @company_forum_topic]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%= f.input :topic_title %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
