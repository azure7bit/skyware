<%= simple_form_for([@company_forum]) do |f| %>
<%= f.error_notification %>
<div class="form-inputs">
    <%= f.input :business_user_id, :as => :hidden, :value => @access.id %>
    <%= f.input :title %>
</div>
<%= f.button :submit, :class => "btn btn-primary" %>
<% end %>
